<style type="text/css">
	p, ul, ol, li, a, span, h1, h2, h3, h4, ul * {
		font-size: 10pt;
		color: #000;
	}

	ul, li {
		margin: 0;
		padding: 0;
	}

	ul {
		padding: 0;
	}

	a, .part_links a {
		color: #333;
		text-decoration: none;
	}

</style>
{{identifierContext=start;name=description}}
<p>{{description}}<br />{{pdf_link}}</p>{{identifierContext=end;name=description}}{{identifierContext=start;name=youtube_video}}
<h4 class="not_to_be_printed">{{writeString=wns.documentation.video_tutorial}} «{{tutorial_name}}»</h4>
{{youtube_video}}{{identifierContext=end;name=youtube_video}}{{identifierContext=start;name=part_links}}
<div class="part_links not_to_be_printed"><h4>{{writeString=wns.documentation.quick_choice_links}}</h4>{{part_links}}</div>{{identifierContext=end;name=part_links}}
{{part}}